import classes from "./Contact.module.css"
import {Col} from "react-bootstrap";

function Contact() {
    return (
        <Col xs={9} className={`text-white ${classes.card}`}>
            contact
        </Col>
    )

}

export default Contact;